# Scanner Capability Comparison Matrix

| Capability Dimension | Current App Scanner (TypeScript / IPC) | UCMA-X (Rust Workspace `ucma-x`) | Shared Code / Dependency | Architecture Difference | Status Verdict |
| :--- | :--- | :--- | :--- | :--- | :--- |
| **Architecture** | Client-side TypeScript Orchestrator + Zustand Store + React UI | Modular Rust workspace (20 crates) + CLI | Tauri IPC bridge (`commands.rs`) | TS handles UI state; Rust provides strict memory-safe causal algorithms | **PARTIAL** |
| **Request Model** | `ParsedHttpRequest` (Method, URL, Headers, Body, Parameters) | `TargetId`, `EndpointId`, `ParameterId`, `AuthorizedRequest` | Serialized JSON DTO over IPC | TS is lexical; Rust is content-derived BLAKE3 typed IDs | **PARTIAL** |
| **Scope Enforcement** | `SafetyController.ts` (Client-side validation gate) | `ucma-scope` (`ScopePolicy`, `SsrfGuard`, IP blocking) | Both enforce explicit operator confirmation | Rust enforces fail-closed SSRF/DNS-rebinding prevention in network layer | **PARTIAL** |
| **HTTP Layer** | Tauri IPC bridge $\to$ `sentinel_repeater` / Tokio reqwest | `ucma-http` (`AuthorizedClient`, DNS pinning, rate limits) | Uses same underlying Tokio Hyper / Reqwest network stack | Unified through Tauri IPC | **SAME** |
| **Parameter Discovery** | `RequestParser.ts` (Query, Path, Cookie, Form, JSON, XML, GraphQL) | `ucma-parameter` (Lexical & AST parameter model) | Same parameter extraction heuristics | TS handles web UI formatting; Rust provides AST traversal | **SAME** |
| **Baseline Modeling** | 3x baseline probe averaging, median latency, body length diff | `ucma-response` normalizer + `ucma-causal` $s_0$ baseline snapshot | Normalization algorithms shared | Rust computes statistical distribution moments; TS uses median/delta | **PARTIAL** |
| **Dynamic Content** | `AdaptivePayloadEngine.stripDynamicContent()` (HTML/JSON diff) | `ucma-response::DynamicContentNormalizer` | Regex and AST dynamic token removal | Implemented in both layers | **SAME** |
| **SQL Context Inference**| `ContextDetector.ts` (Numeric, Quote, Parentheses, Like, OrderBy) | `ucma-parameter::ContextInference` | Identical context classification heuristics | Implemented in both layers | **SAME** |
| **SQL Semantic IR** | String template mutation (`${prefix} AND ${condition}`) | `ucma-sql-ir` AST expression nodes | Dialect syntax definitions | TS uses format strings; Rust uses formal AST tree mutations | **PARTIAL** |
| **Dialect Support** | `DatabaseAdapters.ts` (PG, MySQL, MSSQL, Oracle, SQLite, Db2) | `ucma-dialect` (Postgres, MySQL, MSSQL, Oracle, SQLite) | Identical DBMS dialect queries and functions | Complete cross-engine parity | **SAME** |
| **Error Detection** | `ErrorTester.ts` (30+ regex signatures + Type Conversion Errors) | `ucma-oracles::ErrorOracle` | Identical DBMS error signatures | Complete parity across all 8 DBMS types | **SAME** |
| **Error-Based CAST** | `MetadataExtractor.ts` (PostgreSQL, MSSQL, MySQL, Oracle regexes) | `ucma-explorer::ValueExtractor` (CAST/CONVERT leakage extraction) | Error-based schema and row extraction patterns | Direct error extraction supported in both | **SAME** |
| **Boolean Detection** | `BooleanTester.ts` + `AdaptivePayloadEngine` (Response calibration) | `ucma-oracles::BooleanOracle` + `ucma-causal` 5-step engine | TRUE/FALSE differential pair generators | TS connects to UI live; Rust validates metamorphic invariants | **SAME** |
| **Timing Detection** | `TimeBasedTester.ts` (Median delay thresholding) | `ucma-timing` (EWMA drift model, $3\sigma$ filter) + `ucma-statistics` (SPRT) | Delay injection payloads (`pg_sleep`, `SLEEP`) | Rust implements formal Wald SPRT hypothesis testing | **PARTIAL** |
| **Causal Validation** | 3x reproduction loop (`verifyReproduction`) + Causal tab UI | `ucma-causal::FiveStepCausalEngine` (5-step causal proof) | Bridged via `cmd_ucmax_analyze_boolean` | Rust performs strict causal counterfactual evaluation | **PARTIAL** |
| **Adaptive Scheduling** | `AdaptivePayloadEngine.ts` (Response-driven binary search) | `ucma-planner::AdaptivePlanner` (Bayesian $EIG$ optimizer) | Bridged via `cmd_ucmax_plan_next_step` | Both dynamically adjust test path based on prior observations | **SAME** |
| **Database Explorer** | `SqlScanOrchestrator.ts` + `sqlScannerStore.ts` (Live progressive tree) | `ucma-db` + `ucma-explorer` (Formal DB catalog state machine) | Catalog JSON structure (`DiscoveredTable`, `ColumnMetadata`) | App renders interactive GUI tree; Rust validates schema graph | **SAME** |
| **Evidence & CAS** | `SqlScanFinding.evidence` + BLAKE3 hash generation | `ucma-evidence` (Content-Addressable Store & Merkle DAG) | Uses BLAKE3 digest standard for evidence reproducibility | Cryptographic evidence proof in both systems | **SAME** |
